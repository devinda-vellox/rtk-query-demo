// Shared config object for MSW handlers
// Updated by Leva controls in DevTools component
export const mockConfig = {
  latency: 300, // ms delay for all requests
  errorRate: 0, // 0-100% chance of simulated failure
}
